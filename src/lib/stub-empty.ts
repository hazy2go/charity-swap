// Empty module used to stub out heavy chain SDKs we don't mount yet.
// Wired in next.config.ts. Add a chain SDK alias here only after you've
// removed the matching slot from src/lib/wallet-config.ts.
export {};
export default {};
